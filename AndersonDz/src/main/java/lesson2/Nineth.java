package lesson2;

public class Nineth {
    public static void ninethMethod(int len, int initialValue) {
        int[] newArray = new int[len];

        for(int i = 0; i < newArray.length;i++){
            newArray[i] = initialValue;
        }
    }
}
